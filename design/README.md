# Mandate design system

Approved design deliverables from the design track (branch
`claude/mandate-implementation-spec-vlg13j`, commit `e64bd25`): brand
foundations, tokens (`tokens/`), logo set (`logo/`), and deliverable pages
(`*.dc.html`).

Status notes:

- **Do not consume these tokens in application code yet.** Wiring the design
  system into apps/web is Phase 4/5 work per BUILD-CHECKLIST; until then this
  directory is reference material only.
- **Logo SVGs are not final.** A v1.1 fix from the design team is pending
  (converting live text to outlined paths). Do not embed the current logo
  files in rendered output.
